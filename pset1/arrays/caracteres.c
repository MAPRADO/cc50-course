#include <cs50.h>
#include <stdio.h>

int main(void)
{
    char c = '#';
    //Na verdade não precisamos converter c em int , o compilador faz isso
    //printf ("%i\n", (int) c);
    printf ("%i\n", c);
}