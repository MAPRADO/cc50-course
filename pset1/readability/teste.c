#include <cs50.h>
#include <ctype.h>
#include <stdio.h>
#include <string.h>

int main(void)
{
    string txt = get_string("Digitar uma string de texto: ");
}

char strlen(char *txt)
{
    int total=0;

    while( txt[total] != '\0')
        total++;

    return total;
    printf ("%i\n", strlen(total, txt);
}
